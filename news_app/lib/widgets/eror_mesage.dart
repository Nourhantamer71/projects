import 'package:flutter/widgets.dart';

class ErorMesage extends StatelessWidget {
  const ErorMesage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('there was an error');
  }
}
